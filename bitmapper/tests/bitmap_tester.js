'use strict';

const chai = require('chai');
const expect = chai.expect;
const bitmapReader = require('../bitmap_reader');
const fs = require('fs');

describe('new img created', () => {
  it('should have newImage.bmp', () => {
    if(err) console.log(err);
    
  })
})
